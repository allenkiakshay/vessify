# Database Configuration
DATABASE_URL="your-database-connection-string"

# Better Auth Configuration
BETTER_AUTH_SECRET="generate-a-random-secret-key-at-least-32-characters-long"
BASE_URL="http://localhost:3001"
FRONTEND_URL="http://localhost:3000"

# AWS SES Configuration (for email)
AWS_REGION="ap-south-1"
AWS_ACCESS_KEY_ID="your-aws-access-key-id"
AWS_SECRET_ACCESS_KEY="your-aws-secret-access-key"
AWS_SES_FROM_EMAIL="noreply@yourdomain.com"

# AWS Bedrock Configuration (for AI transaction extraction)
# Using Claude Sonnet 4.5 via global inference profile
# Model: global.anthropic.claude-sonnet-4-5-20250929-v1:0
# Region: ap-south-1 (Mumbai)
# Same AWS credentials as above, ensure IAM user/role has bedrock:InvokeModel permission
# If not configured, the system will fall back to regex-based parsing

# Node Environment
NODE_ENV="development"
